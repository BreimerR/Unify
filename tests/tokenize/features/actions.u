class Foo{

    getUserNames(actionData){

    }

    reset{

    }
}


fun main(...String args){
    foo = new Foo()
    // this is when the function does not require parameters
    // to perform it's actions
    // else functions should be declared with either one or more parameters
    foo->reset
}