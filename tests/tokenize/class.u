class Foo<V> {

    <T : V> update(val T int : (10 * 10)  :: this.validator,failHandler)  {

        when a += {
            age {
                var age  = age age age

                age = age ? true : new Map()

                var name = new App()
            }
        }
    }

    var a = 20 times name

    name(){

    }

}

/**/
