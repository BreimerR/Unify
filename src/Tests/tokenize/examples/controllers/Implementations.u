class Implementations{

   Float age = 12;

   private place = "we";

   protected selection = 1;

   get age => this.age;

    /*
    *   Can only access protected and public properties.
    */
   __get(name) => this.$name;

   __set(name,value) => {
      try{
          this.$name = value;
      }catch {
        return false
      }
   }

    /*
    *   Can only access protected and public functions
    */
   __call(){

   }
}
